# Handovers: To Backend Developer

## 날짜
2025-12-08

## 현재 상황 (Context)
- Docker 인프라가 정상 가동 중이며, Alembic 마이그레이션 파일들이 준비되어 있습니다 (`initial_schema`, `add_position_table`).
- API 엔드포인트(`assets`, `positions`) 구현이 완료된 상태입니다.

## 해야 할 일 (Tasks)
1. **DB 마이그레이션 적용**:
   - `alembic upgrade head` 명령어를 실행하여 DB 스키마를 최신 상태로 동기화합니다.
2. **기본 데이터 검증**:
   - `GET /api/v1/assets` 및 `GET /api/v1/positions` 엔드포인트가 정상 응답(200 OK)하는지 확인합니다.
3. **Seed 데이터 추가 (선택)**:
   - 데이터가 비어있다면 테스트를 위해 임의의 자산(Asset) 1개를 추가해봅니다.

## 기대 산출물 (Expected Outputs)
- DB에 테이블이 생성되고, API 호출 시 에러 없이 응답.

## 참고 자료 (References)
- `backend/alembic/versions/`
- `backend/app/api/endpoints/`
