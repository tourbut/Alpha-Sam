# Handovers: To QA Tester

## 날짜
2025-12-07

## 현재 상황 (Context)
- 백엔드: 자산 CRUD (`/assets`) 및 시세 업데이트 (`/prices/refresh`) API 구현 완료.
- 프론트엔드: 자산 목록 및 추가 UI 구현 완료 (Price 연동은 아직 미적용일 수 있음).
- 전체적인 기능 연동 상태를 점검해야 합니다.

## 해야 할 일 (Tasks)
1. **서버 구동 확인**:
   - Backend: `uvicorn app.main:app --reload`
   - Frontend: `npm run dev`
2. **API 기능 테스트 (Swagger UI 활용)**:
   - `POST /api/v1/assets`: 자산(예: BTC, AAPL) 등록 테스트.
   - `POST /api/v1/prices/refresh`: 시세 업데이트 실행 -> 200 OK 확인.
   - `GET /api/v1/assets`: 응답에 `latest_price` 필드가 포함되어 있고 값이 채워져 있는지 확인.
3. **UI 기능 테스트**:
   - `/assets` 접속하여 등록한 자산이 리스트에 뜨는지 확인.
   - (참고) UI에 아직 가격이 안 뜰 수 있음. 이 경우 "UI 미연동"으로 리포트하고, 백엔드 기능 정상 여부만 집중 검증.

## 기대 산출물 (Expected Outputs)
- QA 리포트 (`.artifacts/handovers/logs/2025-12-07_qa_report.md`에 작성 권장).
- 발견된 버그나 개선 필요 사항 정리.

## 참고 자료 (References)
- `.artifacts/prompts/projects/alpha_sam/context.md`
