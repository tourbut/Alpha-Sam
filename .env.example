# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=alpha_sam
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Backend
BACKEND_PORT=8000
SECRET_KEY=your_secret_key_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
# DATABASE_URL and REDIS_URL are typically constructed in docker-compose, but can be overridden here if needed

# SMTP / Email
EMAILS_ENABLED=true
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@alphasam.com

# MailHog
MAILHOG_UI_PORT=8025
MAILHOG_SMTP_PORT=1025

# Frontend
ORIGIN=http://localhost

# Nginx
NGINX_PORT=80
