# Handovers: To QA Tester

## 날짜
2025-12-11

## 현재 상황 (Context)
- 개발 환경 검증이 완료되었으며, DevOps가 로컬 프로덕션 환경(`:4173`)을 준비 중입니다.
- 프로덕션 빌드에서 기능이 동일하게 동작하는지 Smoke Test가 필요합니다.

## 해야 할 일 (Tasks)
1. **프로덕션 환경 접속**:
   - `http://localhost:4173` 접속 (DevOps 작업 완료 후).
2. **Smoke Test**:
   - 로그인 가능 여부 확인.
   - 자산 목록 로딩 및 포트폴리오 요약 수치 확인.
   - *주의: 프로덕션 DB(`postgres_data`)가 기존 개발 DB와 볼륨을 공유하는지, 분리되었는지(비어있는지) 확인 필요.*

## 기대 산출물 (Expected Outputs)
- 프로덕션 환경 검증 결과 리포트.

## 참고 자료 (References)
- `handovers/to_devops.md`
# Handovers: To QA Tester

## 날짜
2025-12-11

## 현재 상황 (Context)
- 프론트엔드 UI/UX가 개선되었습니다.
  1. **Fixed Navbar**: 스크롤을 내려도 네비게이션 바가 상단에 고정됩니다.
  2. **Auth Redirect**: 비로그인 상태에서 대시보드(`/`) 접근 시 로그인 페이지(`/login`)로 자동 이동합니다.

## 해야 할 일 (Tasks)
1. **Navbar 고정 테스트**:
   - `/assets` 등 스크롤이 발생하는 페이지에서 스크롤 다운.
   - 네비게이션 바가 사라지지 않고 상단에 붙어있는지 확인.
   - 본문 내용이 네비게이션 바에 가려지지 않는지(상단 여백 `pt-20` 적용 여부) 확인.
2. **리다이렉트 테스트**:
   - 로그아웃 상태에서 브라우저 주소창에 `http://localhost:5173/` 입력.
   - 자동으로 `/login` 페이지로 이동하는지 확인.
   - 로그인 후에는 정상적으로 대시보드가 뜨는지 확인.

## 기대 산출물 (Expected Outputs)
- QA 리포트 (`.artifacts/handovers/logs/2025-12-11_qa_ui_report.md`).
- 개선된 UI 동작 확진.

## 참고 자료 (References)
- `frontend/src/routes/+layout.svelte`
- `frontend/src/routes/+page.svelte`
# Handovers: To QA Tester

## 날짜
2025-12-11

## 현재 상황 (Context)
- 대시보드(`/`)에 Chart.js 기반의 차트가 추가되었습니다.
  1. **Portfolio Distribution (Pie Chart)**: 자산 구성 비중.
  2. **Portfolio History (Line Chart)**: 수익률 추이 (현재 Mock Data).

## 해야 할 일 (Tasks)
1. **차트 렌더링 확인**:
   - 대시보드 페이지 접속 시 두 개의 차트가 정상적으로 표시되는지 확인.
   - 브라우저 콘솔에 Chart.js 관련 에러(Canvas context 등)가 없는지 확인.
2. **반응형 동작 확인**:
   - 브라우저 창 크기를 조절했을 때 차트 크기가 적절히 리사이징되는지 확인.
3. **데이터 정합성 (Pie Chart)**:
   - "Distribution" 차트의 데이터가 현재 보유한 자산(Assets)의 평가액(Valuation) 비중과 일치하는지 대략적으로 확인.
   - (참고: 값이 0이거나 데이터가 없으면 빈 차트 또는 메시지가 나와야 자연스러움).

## 기대 산출물 (Expected Outputs)
- QA 리포트 (`.artifacts/handovers/logs/2025-12-11_qa_analytics_report.md`).
- 차트 기능 정상 동작 확진.

## 참고 자료 (References)
- `frontend/src/lib/components/PortfolioDistributionChart.svelte`
- `frontend/src/lib/components/PortfolioHistoryChart.svelte`
